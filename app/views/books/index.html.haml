%p{:id => "notice"}= notice

%h1 Listing books

%table
  %thead
    %tr
      %th Title
      %th Price
      %th Books in stock
      %th{:colspan => "3"}
    
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.price
        %td= book.books_in_stock
        %td= link_to 'Details', book
%br

= link_to 'New Book', new_book_path
