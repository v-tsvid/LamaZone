.row-fluid
  .col-md-6.col-md-offset-3
    %h4
      %span New review for
      = link_to "#{@book.title} by #{@book.author.full_name}", @book

    = form_for [@book, @rating] do |f|
      = render 'form', f: f


