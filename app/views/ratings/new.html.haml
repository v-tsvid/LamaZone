.row-fluid
  .col-md-6.col-md-offset-3
    %h4
      %span= t "new_review_for"
      = link_to "#{@book.title} #{t(:by_author)} #{@book.author.full_name}", @book

    = form_for [@book, @rating] do |f|
      = render 'form', f: f


